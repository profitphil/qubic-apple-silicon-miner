# build artifacts (rebuild with each project's build.sh / cmake)
qiner-macos/build/
qiner-metal/build/
qiner-metal/metal-hello/hello
qiner-metal/metal-hello/stress
*.o
*.dSYM/

# macOS noise
.DS_Store

# session-specific working files (local only, not part of the published project)
CONTINUE-METAL-PORT.md
tools/

# NEVER COMMIT: contains real pool access tokens / credentials
deploy/
mine.log

# stray scratchpad fixtures
scratchpad_*

# live task (public core file, fetched at runtime)
*.task
stratum-live.log

# per-user token config
qli-config.json
live.log
